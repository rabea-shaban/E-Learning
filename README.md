# E-Learning

A React + json-server prototype for browsing courses, watching lessons, and tracking progress.

## Getting Started

```bash
npm install
```

### Run the mock API

```bash
npm run server
```

### Run the frontend

```bash
npm run dev
```

The frontend expects the API at `http://localhost:3001`. You can adjust it via `.env`.
